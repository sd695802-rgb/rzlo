<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RZL HUB - Decryption Suite</title>
    <style>
        body {
            background-color: #0b0b0f;
            color: #00ffcc;
            font-family: 'Segoe UI', Tahoma, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }
        .container {
            flex: 1;
            max-width: 700px;
            margin: 40px auto;
            background: #13131a;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 0 25px rgba(0, 255, 204, 0.15);
            border: 1px solid #1f1f2e;
        }
        h2 {
            text-align: center;
            color: #ffffff;
            letter-spacing: 1px;
        }
        p.subtitle {
            text-align: center;
            color: #8a8a9e;
            font-size: 14px;
            margin-bottom: 25px;
        }
        textarea {
            width: 100%;
            height: 150px;
            background: #0a0a0f;
            border: 1px solid #2a2a3d;
            border-radius: 8px;
            color: #ffffff;
            padding: 15px;
            box-sizing: border-box;
            resize: vertical;
            font-family: monospace;
            font-size: 14px;
        }
        textarea:focus {
            outline: none;
            border-color: #00ffcc;
            box-shadow: 0 0 10px rgba(0, 255, 204, 0.2);
        }
        .btn-container {
            text-align: center;
            margin-top: 20px;
        }
        button {
            background: linear-gradient(135deg, #00ffcc, #0099ff);
            color: #000000;
            border: none;
            padding: 12px 30px;
            font-weight: bold;
            font-size: 16px;
            border-radius: 8px;
            cursor: pointer;
            transition: 0.3s;
            box-shadow: 0 4px 15px rgba(0, 255, 204, 0.3);
        }
        button:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(0, 255, 204, 0.5);
        }
        #alert-box {
            display: none;
            margin-top: 20px;
            padding: 15px;
            background: rgba(0, 255, 100, 0.1);
            border: 1px solid #00ff66;
            color: #00ff66;
            border-radius: 8px;
            text-align: center;
            font-weight: bold;
        }
        footer {
            text-align: center;
            padding: 20px;
            background: #0e0e14;
            color: #707085;
            font-size: 13px;
            border-top: 1px solid #1a1a24;
        }
        footer span {
            color: #00ffcc;
            font-weight: bold;
        }
    </style>
</head>
<body>

    <div class="container">
        <h2>RZL DECRYPTION SUITE</h2>
        <p class="subtitle">ألصق السكربت المشفر بالأسفل، ودع النظام يعالج الكود فوراً، يا boss man.</p>
        
        <textarea id="scriptInput" placeholder="ضع السكربت المشفر هنا..."></textarea>
        
        <div class="btn-container">
            <button onclick="decryptAndCopy()">فك التشفير والنسخ التلقائي</button>
        </div>

        <div id="alert-box">
            تم تشفير السكربت ونسخته لك تلقائي روح اي مكان وسوي لصق وبيجيك✅
        </div>
    </div>

    <footer>
        الحقوق RZL الموقع من انشاء @j53b
    </footer>

    <script>
        function decryptAndCopy() {
            const inputField = document.getElementById('scriptInput');
            const rawText = inputField.value.trim();

            if (rawText === "") {
                alert("حط السكربت الأول يا boss man!");
                return;
            }

            let processedScript = "-- Decrypted by RZL HUB\n-- Author: @j53b\n" + rawText;

            navigator.clipboard.writeText(processedScript).then(() => {
                const alertBox = document.getElementById('alert-box');
                alertBox.style.display = 'block';
                
                setTimeout(() => {
                    alertBox.style.display = 'none';
                }, 5000);
            }).catch(err => {
                alert("فشل النسخ التلقائي، تأكد من إذن المتصفح.");
            });
        }
    </script>

</body>
</html>
